# End-to-end tests

This directory will hold deterministic Windows, Chrome, and Edge acceptance tests as vertical slices are implemented.

Tests must use disposable fixtures. They must never require real credentials, production services, or external authentication accounts.
