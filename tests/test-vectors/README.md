# Security test vectors

No cryptographic test vectors are defined yet. Issue [#9](https://github.com/theundeadmonk/Librarian/issues/9) must first approve the key hierarchy, algorithms, record format, and corruption behavior.

Future vectors must be deterministic, non-secret, versioned, and independently reviewable.
