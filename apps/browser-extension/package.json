{
  "name": "@librarian/browser-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome and Edge integration boundary for Librarian",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json && tsc --project tsconfig.content.json && node build.mjs",
    "check": "tsc --project tsconfig.json --noEmit && tsc --project tsconfig.content.json --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs",
    "test:browser-dom": "npm run build && node tests/browser-dom.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "typescript": "7.0.2"
  }
}
